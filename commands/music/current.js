module.exports = {
    name: 'current',
    description: 'Displays the current song',
    aliases: ['np'],
    cooldown: 3,
    guildOnly: true,
    args: false,

    async execute(message, args) {
        
    }
}