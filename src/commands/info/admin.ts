import { CommandInfo } from '../../classes/CommandInfo';

export const info: CommandInfo = {
    name: 'Admin',
    description: 'Admin Commands',
    aliases: ['sys'],
    commands: [],
    perms: ['admin']
}
