import { CommandInfo } from '../../classes/CommandInfo';

export const info: CommandInfo = {
   name: 'Blocked',
   description: 'Block users from using bot commands',
   commands: [],
   aliases: [],
   override: 'list',
   perms: ['mod', 'kira']
};
